# Supabase (Frontend client keys — publishable only)
# Get from Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://sijioycftrrdugxuhttq.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Direct Postgres connection (for Supabase CLI, migrations, psql, server-side tools ONLY)
# Never use in frontend code or commit the password.
# SUPABASE_DB_URL=postgresql://postgres:YOUR_PASSWORD@db.sijioycftrrdugxuhttq.supabase.co:5432/postgres

# The Odds API (if using live data)
# Get a free key at https://the-odds-api.com/
# Then put it here as VITE_ODDS_API_KEY=yourkey
# The app prefers a key saved via the "Connect Live Data" UI (localStorage) over this env var.
# VITE_ODDS_API_KEY=your-key-here
